# SwiftrideX Frontend

React + Vite frontend for the SwiftrideX ride-hailing experience.

## Development

1. Install dependencies.
2. Start the backend on port 5000.
3. Run `npm run dev` from this folder.

## Environment

Copy `.env.example` to `.env` and adjust the API/socket URLs if needed.
